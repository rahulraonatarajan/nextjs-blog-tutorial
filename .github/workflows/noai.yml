  - name: NoAI Guardian
  uses: rahulraonatarajan/ai-guardian@LLMs
  with:
    # Folder (relative to repo root) to scan
    path: # optional, default is .
    # Set to true to auto-patch violations
    fix: # optional, default is false
          
